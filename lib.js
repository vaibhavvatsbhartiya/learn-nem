// Modules bn giye hai yeh
// we have to export each module if we need to use them in other files 

export const sum = (a,b) =>{
    return a + b ;
}

export const diff = (a,b) =>{
    return a - b ;
}